import { readFileSync } from "node:fs";import { resolve } from "node:path";import { describe,expect,it } from "vitest";import { auditCurrentVerticals,type VerticalConfig } from "../../scripts/seo/audit-vertical-modules";
const config=JSON.parse(readFileSync(resolve(process.cwd(),"sites/cbamvalid/seo.config.json"),"utf8")) as VerticalConfig;
describe("Phase 15 vertical modules",()=>{it("activates only configured SaaS module",()=>{const r=auditCurrentVerticals(config);expect(r.activeVerticals).toEqual(["saas"]);expect(r.moduleState.find(x=>x.vertical==="saas")?.status).toBe("PASS");expect(r.moduleState.filter(x=>x.vertical!=="saas").every(x=>x.status==="SKIP_NOT_ACTIVE")).toBe(true)});it("keeps public commercial offers on verified SSOT",()=>{const product=readFileSync(resolve(process.cwd(),"app/(public)/product/page.tsx"),"utf8");const pricing=readFileSync(resolve(process.cwd(),"app/(public)/pricing/page.tsx"),"utf8");const schema=readFileSync(resolve(process.cwd(),"lib/seo/schema.ts"),"utf8");expect(product).toContain("CANONICAL_PRICING");expect(pricing).toContain("CANONICAL_PRICING");expect(schema).toContain("PRICE_CLAIM");expect(schema).toContain('availability: "https://schema.org/InStock"')});});