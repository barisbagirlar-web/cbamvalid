import { readFileSync } from "node:fs";import { resolve } from "node:path";import { describe,expect,it } from "vitest";import { assertTruthfulSaasOffer } from "../../scripts/seo/audit-vertical-modules";
describe("INV-15.8",()=>{it("blocks invented price/trial",()=>expect(()=>assertTruthfulSaasOffer({visiblePriceMinor:1,canonicalPriceMinor:2,visibleTrialClaim:"free-trial",verifiedTrialClaim:"free-draft"})).toThrow(/INV-15\.8/));it("current product/pricing consume canonical pricing SSOT",()=>{for(const p of ["app/(public)/product/page.tsx","app/(public)/pricing/page.tsx"]){expect(readFileSync(resolve(process.cwd(),p),"utf8")).toContain("CANONICAL_PRICING")}})});