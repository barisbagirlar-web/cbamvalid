import { readFileSync } from "node:fs";import { resolve } from "node:path";import { describe,expect,it } from "vitest";import { assertSaasMethodologySsr } from "../../scripts/seo/audit-vertical-modules";
describe("INV-15.7",()=>{it("passes current methodology server surface",()=>{const s=readFileSync(resolve(process.cwd(),"app/(public)/methodology/page.tsx"),"utf8");expect(()=>assertSaasMethodologySsr(s)).not.toThrow()});it("blocks client-only methodology fixture",()=>expect(()=>assertSaasMethodologySsr('"use client"; export default function Page(){return <main><h1>x</h1></main>}')).toThrow(/INV-15\.7/))});