.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example
node_modules/
**/node_modules/
.venv/
**/.venv/
.next/
**/.next/
.firebase/
**/.firebase/
out/
dist/
build/
functions/build/
functions/lib/
coverage/
playwright-report/
.playwright/
wizard-auth.json
test-results/
.gemini/
*.log
*.pem
*.key
service-account*.json
firebase-adminsdk*.json
.DS_Store
tsconfig.tsbuildinfo

# Firebase Admin credentials
firebase-admin-key.json
**/firebase-admin-key.json
firebase-adminsdk*.json
service-account*.json

# Local test artifacts
test-out.pdf
test-page-*.png
test-page-0.png
test-page-1.png
test_sips.png
test-raster.ts
CBAMValid_English_A_to_Z_Walkthrough.mp4.png
create-admin-routes.sh
artifacts/render-qa/
# Emulator-only permissive rules fixture — never commit; production uses firestore.rules
scripts/firestore-rules-permissive.rules
# Generated four-complete-dossier render output (reproducible via npm run render:four-complete-dossiers)
artifacts/four-complete-dossiers/
# Step 8 final-action screenshot harness output (reproducible via the local E2E harness)
artifacts/step8-final-action/
# G-09 rendered V6 package byproduct (reproducible via npm run render:v6-gate-package);
# the gate evidence itself (scan-result.txt / scan-report.json) is committed.
artifacts/gates/G-09/package/
# teb232 test hesabı sıfırlama backup çıktısı (scripts/reset-teb232-two-scenarios.ts ile üretilir)
artifacts/teb232-reset/
tsconfig.check.tsbuildinfo

# Local SEO lab CWV scratch (baseline lives in docs/seo/)
.seo-cwv/

# Paddle credentials handoff file — contains live API keys; never commit.
CBAM-PADDLE-*.txt

# Kimi agent ERP görünümü/kullanıcı arayüzü teslimat paketi
# (patch bundle'ı + Vite prototipi — RM-008/009 teslim artefaktı, repo'ya girmez)
# Glob covers the base folder and Finder " (1)" / " (2)" duplicates.
Kimi_Agent_ERP*/
