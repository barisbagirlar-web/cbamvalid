# Firebase Framework-Aware Hosting 0.11.8 declares an obsolete optional Sharp
# peer range (^0.32 || ^0.33), while Next 16 requires Sharp ^0.34.5.
# Runtime/build compatibility is verified; let each package resolve its own peer.
legacy-peer-deps=true
